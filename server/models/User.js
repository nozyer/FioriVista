class User {
  constructor(userId, userEmail, userRole, userName, userAddress) {
    this.userEmail = userEmail;
    this.userId = userId;
    this.userName = userName;
    this.userRole = userRole;
    this.userAddress = userAddress;
  }
}
